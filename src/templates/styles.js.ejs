import { Platform } from 'react-native';

const styles = {
    container: {
        flexGrow: 1,
        flexDirection: 'column',
        justifyContent: 'center',
        alignItems: 'center',
        width: "100%"
    }
};

export default styles;